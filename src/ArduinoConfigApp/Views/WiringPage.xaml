<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ArduinoConfigApp.Views.WiringPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <Grid Padding="24" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock
                    Text="Wiring Guide"
                    Style="{StaticResource TitleTextBlockStyle}"
                    Margin="0,0,0,8"/>
                <TextBlock
                    x:Name="SummaryText"
                    Text="View wiring diagrams and step-by-step instructions"
                    Style="{StaticResource BodyTextBlockStyle}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <Button Click="CopyToClipboard_Click" ToolTipService.ToolTip="Copy wiring guide to clipboard">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8C8;" FontSize="16"/>
                        <TextBlock Text="Copy Guide"/>
                    </StackPanel>
                </Button>
                <Button Click="Refresh_Click" ToolTipService.ToolTip="Refresh diagram">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE72C;" FontSize="16"/>
                        <TextBlock Text="Refresh"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="400"/>
            </Grid.ColumnDefinitions>

            <!-- Wiring Diagram -->
            <Border
                Grid.Column="0"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="20"
                Margin="0,0,16,0">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Wiring Diagram" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,16"/>

                    <!-- Empty State -->
                    <StackPanel
                        x:Name="EmptyDiagramPanel"
                        Grid.Row="1"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Spacing="16"
                        Visibility="Collapsed">
                        <FontIcon Glyph="&#xE8A5;" FontSize="48" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock
                            Text="No components configured"
                            Style="{StaticResource SubtitleTextBlockStyle}"
                            HorizontalAlignment="Center"/>
                        <TextBlock
                            Text="Add inputs or displays to see wiring diagram"
                            Style="{StaticResource BodyTextBlockStyle}"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            HorizontalAlignment="Center"/>
                    </StackPanel>

                    <!-- Diagram Content -->
                    <ScrollViewer x:Name="DiagramScrollViewer" Grid.Row="1" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <Grid x:Name="DiagramGrid" MinWidth="600" MinHeight="400">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- Board Visualization -->
                            <Border
                                x:Name="BoardVisual"
                                Grid.Row="0"
                                Background="#1E3A5F"
                                BorderBrush="#2E5A8F"
                                BorderThickness="2"
                                CornerRadius="4"
                                Padding="16"
                                HorizontalAlignment="Center"
                                Margin="0,0,0,24">

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock
                                        x:Name="BoardNameText"
                                        Grid.Row="0"
                                        Text="Arduino Pro Micro"
                                        FontWeight="Bold"
                                        Foreground="White"
                                        HorizontalAlignment="Center"
                                        Margin="0,0,0,12"/>

                                    <!-- Pin Headers -->
                                    <StackPanel x:Name="LeftPinsPanel" Grid.Row="1" Orientation="Horizontal" Spacing="4" HorizontalAlignment="Center"/>
                                    <StackPanel x:Name="RightPinsPanel" Grid.Row="2" Orientation="Horizontal" Spacing="4" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                                </Grid>
                            </Border>

                            <!-- Components List Visual -->
                            <ItemsControl x:Name="ComponentsVisual" Grid.Row="1">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Spacing="12"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </Grid>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Step-by-Step Guide -->
            <Border
                Grid.Column="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="20">

                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Step-by-Step Guide" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,16"/>

                    <!-- Legend -->
                    <Border Grid.Row="1" Background="{ThemeResource SubtleFillColorSecondaryBrush}" CornerRadius="6" Padding="12" Margin="0,0,0,16">
                        <StackPanel Spacing="8">
                            <TextBlock Text="Wire Colors" Style="{StaticResource CaptionTextBlockStyle}" FontWeight="SemiBold"/>
                            <StackPanel Orientation="Horizontal" Spacing="16">
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <Border Background="#E74C3C" Width="16" Height="16" CornerRadius="2"/>
                                    <TextBlock Text="VCC/5V" Style="{StaticResource CaptionTextBlockStyle}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <Border Background="#2C3E50" Width="16" Height="16" CornerRadius="2"/>
                                    <TextBlock Text="GND" Style="{StaticResource CaptionTextBlockStyle}"/>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Spacing="16">
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <Border Background="#3498DB" Width="16" Height="16" CornerRadius="2"/>
                                    <TextBlock Text="Signal" Style="{StaticResource CaptionTextBlockStyle}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <Border Background="#F39C12" Width="16" Height="16" CornerRadius="2"/>
                                    <TextBlock Text="SPI" Style="{StaticResource CaptionTextBlockStyle}"/>
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Steps List -->
                    <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="StepsPanel" Spacing="12">
                            <!-- Steps will be added programmatically -->
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
